# Git

Notas de Git, para me auxiliar

```
// Tags
git tag -a 0.0.1 -m "versão 0.0.1"
git push origin 0.0.1

// Deletar Tag
git tag -d 0.0.1

// Branch
git checkout nome-da-branch

// Commits
git pull
git status
git add .
git commit -m "mensagem"
git push
```